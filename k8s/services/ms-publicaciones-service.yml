apiVersion: v1
kind: Service
metadata:
  name: ms-publicaciones-service
  namespace: distri-libreria
spec:
  selector:
    app: ms-publicaciones
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

