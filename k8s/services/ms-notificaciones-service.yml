apiVersion: v1
kind: Service
metadata:
  name: ms-notificaciones-service
  namespace: distri-libreria
spec:
  selector:
    app: ms-notificaciones
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

