apiVersion: v1
kind: Service
metadata:
  name: ms-usuarios-service
  namespace: distri-libreria
spec:
  selector:
    app: ms-usuarios
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

